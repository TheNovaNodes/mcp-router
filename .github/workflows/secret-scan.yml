name: 🛡️ Security Audit

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  secret-scan:
    uses: TheNovaNodes/.github/.github/workflows/reusable-secret-scan.yml@master
    secrets: inherit
